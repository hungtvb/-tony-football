# Release Process

`main` → sprint branch → implementation → automated checks → manual gameplay validation → PR → review → explicit merge approval → GitHub Pages verification → changelog/context update.

Release validation covers page load, WebGL, Canvas fallback, keyboard controls, model fallback, pause, restart, replay, and result flow.