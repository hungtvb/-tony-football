# Review Guidelines

Review in this order: scope, architecture, regression, tests, performance, readability, documentation.

Block a PR when it implements beyond the active sprint, adds another large subsystem to `game.js`, changes controls without approval, removes fallback behavior, gives AI hidden physical boosts, omits practical tests, or contradicts documentation.