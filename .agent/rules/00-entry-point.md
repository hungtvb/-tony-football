# Tony Football Antigravity Entry Rule

Always read the repository root `AGENTS.md` before planning or editing.

Then follow the mandatory read order defined there.

Treat `docs/` as the authoritative project workspace. Do not duplicate project specifications inside `.agent/`.