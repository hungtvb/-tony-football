name: Job write
on: [workflow_dispatch]
permissions:
  contents: read
jobs:
  publish:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - run: gh release create v1 asset.zip
