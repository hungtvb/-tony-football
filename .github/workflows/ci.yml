name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: feature/u3-1-camera-hud
          fetch-depth: 0
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Apply guarded U3.1 HUD polish integration
        run: |
          if ! grep -q 'hints-dimmed' game.js; then
            node scripts/apply-u3-1-hud-polish.mjs
          fi
      - name: Run test suite
        run: npm test
      - name: Commit integrated HUD polish
        run: |
          if ! git diff --quiet -- game.js; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add game.js
            git commit -m "feat(u3.1): integrate HUD hierarchy states"
            git push origin HEAD:feature/u3-1-camera-hud
          fi
