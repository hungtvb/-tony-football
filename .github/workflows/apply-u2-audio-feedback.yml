name: Apply U2 audio feedback

on:
  push:
    branches:
      - feature/ui-u2-game-feel
    paths:
      - scripts/apply-u2-audio-feedback.mjs
      - .github/workflows/apply-u2-audio-feedback.yml

permissions:
  contents: write

jobs:
  integrate:
    if: ${{ !contains(github.event.head_commit.message, '[u2-audio-applied]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/ui-u2-game-feel
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Apply guarded migration
        run: node scripts/apply-u2-audio-feedback.mjs
      - name: Run full test suite
        run: npm test
      - name: Commit integration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add game.js
          git commit -m "feat(audio): integrate cooldown-based feedback [u2-audio-applied]"
          git push origin HEAD:feature/ui-u2-game-feel
